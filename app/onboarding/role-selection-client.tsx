// ROLE PICKER DISABLED.
// Original code preserved in role-selection-client.original.tsx
// Re-enable: restore this file from role-selection-client.original.tsx
// and update app/onboarding/page.tsx per the comments there.

export default function RoleSelectionClient({ userName }: { userName: string }) {
  // Disabled — see app/onboarding/page.tsx
  return null;
}
