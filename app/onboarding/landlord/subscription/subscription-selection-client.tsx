// SUBSCRIPTION SELECTION UI DISABLED — card wall moved to /admin/billing.
// Original code preserved in subscription-selection-client.original.tsx
// Re-enable: restore this file from subscription-selection-client.original.tsx
// and update app/onboarding/landlord/subscription/page.tsx per comments there.

export default function SubscriptionSelectionClient({ userName }: { userName: string }) {
  // Disabled — see app/onboarding/landlord/subscription/page.tsx
  return null;
}
